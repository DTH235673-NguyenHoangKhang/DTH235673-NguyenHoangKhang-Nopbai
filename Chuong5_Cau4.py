bai4 = "Câu 4: Các hàm xử lý chuỗi trong Python\n"

bai4 += "len(): Trả về độ dài của chuỗi.\n\n"

bai4 += "lower(), upper(): Chuyển chuỗi về chữ thường hoặc in hoa.\n\n"

bai4 += "strip(), lstrip(), rstrip(): Xoá khoảng trắng ở đầu, cuối hoặc cả hai bên chuỗi.\n\n"

bai4 += "replace(old, new): Thay thế chuỗi con cũ bằng chuỗi con mới.\n\n"

bai4 += "find(sub), index(sub): Tìm vị trí xuất hiện đầu tiên của chuỗi con.\n"
bai4 += "→ find() trả về -1 nếu không tìm thấy, index() sẽ báo lỗi.\n\n"

bai4 += "split(sep): Tách chuỗi thành danh sách dựa trên ký tự phân cách.\n\n"

bai4 += "join(list): Nối các phần tử trong danh sách thành chuỗi.\n\n"

bai4 += "startswith(prefix), endswith(suffix): Kiểm tra chuỗi có bắt đầu hoặc kết thúc bằng chuỗi con nào đó không.\n\n"

bai4 += "isdigit(), isalpha(), isalnum(): Kiểm tra chuỗi có phải là số, chữ cái, hoặc chữ cái/số kết hợp.\n\n"

bai4 += "capitalize(), title(): Viết hoa chữ cái đầu chuỗi hoặc đầu mỗi từ.\n\n"

bai4 += "format(), f-string: Định dạng chuỗi động.\n"

print(bai4)
